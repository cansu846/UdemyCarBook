@using UdemyCarBook.Dtos.FeatureDtos
@model List<ResultFeatureDto>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/AdminLayout/Index.cshtml";
    var count = 0;
}
<div class="page-content">
    <div class="container-fluid">
        <div class="row">
            <div class="col-xl-12">
                <div class="card">
                    <div class="card-body">
                        <h4 class="card-title">Car Features</h4>
                        <p class="card-subtitle mb-4">
                           
                        </p>
                        <div class="table-responsive">
                            <table class="table mb-0">
                                <thead>
                                    <tr>
                                        <th>#</th>
                                        <th>Name</th>
                                        <th>Update</th>
                                        <th>Detail</th>
                                        <th>Delete</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var item in Model)
                                    {
                                        count++;

                                        <tr>
                                            <th>@count</th>
                                            <td>@item.FeatureName</td>
                                         
                                            <td><a asp-area="Admin" asp-controller="Feature" asp-action="UpdateFeature" asp-route-id="@item.FeatureId" class="btn btn-success">Update</a></td>
                                            <td><a href="" class="btn btn-primary">Go to detail</a></td>
                                            <td><a href="/Admin/Feature/DeleteFeature/@item.FeatureId" class="btn btn-danger">Delete</a></td>
                                        </tr>
                                    }

                                    <tr>

                                </tbody>
                            </table>
                            <a asp-area="Admin" asp-controller="Feature" asp-action="CreateFeature" class="btn btn-primary">Create Feature</a>
                        </div>

                    </div>
                    <!-- end card-body-->
                </div>
                <!-- end card -->

            </div>
            <!-- end col -->

        </div>

    </div>
</div>

