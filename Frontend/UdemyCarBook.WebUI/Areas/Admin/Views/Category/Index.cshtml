@using UdemyCarBook.Dtos.CategoryDtos
@model List<ResultCategoryDto>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/AdminLayout/Index.cshtml";
    var count = 0;
}
<div class="page-content">
    <div class="container-fluid">
        <div class="row">
            <div class="col-xl-12">
                <div class="card">
                    <div class="card-body">
                        <h4 class="card-title">Car Categorys</h4>
                        <p class="card-subtitle mb-4">

                        </p>
                        <div class="table-responsive">
                            <table class="table mb-0">
                                <thead>
                                    <tr>
                                        <th>#</th>
                                        <th>Name</th>
                                        <th>Blogs of Categories</th>
                                        <th>Update</th>
                                        <th>Delete</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var item in Model)
                                    {
                                        count++;

                                        <tr>
                                            <th>@count</th>
                                            <td>@item.name</td>
                                            <td><a href="#" class="btn btn-info">Blogs of Categories</a></td>
                                            <td><a asp-area="Admin" asp-controller="Category" asp-action="UpdateCategory" asp-route-id="@item.categoryForBlogId" class="btn btn-success">Update</a></td>
                                            <td><a href="/Admin/Category/DeleteCategory/@item.categoryForBlogId" class="btn btn-danger">Delete</a></td>
                                        </tr>
                                    }

                                    <tr>

                                </tbody>
                            </table>
                            <a asp-area="Admin" asp-controller="Category" asp-action="CreateCategory" class="btn btn-primary">Create Category</a>
                        </div>

                    </div>
                    <!-- end card-body-->
                </div>
                <!-- end card -->

            </div>
            <!-- end col -->

        </div>

    </div>
</div>

